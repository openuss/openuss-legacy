/* Generated by Freestyle Learning Group */

package freestyleLearningGroup.independent.plotter;

import java.awt.Color;

/**
 * <code>FLGData2D</code> repr&auml;sentiert ein Datum bestehend aus einem Namen, einem
 * zugeordneten Wert und evtl. einer Farbe. <p> <b>Beispiel</b>: Erzeugt drei verschiedene
 * <code>FLGData2D</code>-Objekte. <p><pre>
 *		FLGData2D a = new FLGData2D("Alex", 25, java.awt.Color.green);
 *		FLGData2D b = new FLGData2D("Andrea", 32, java.awt.Color.red);
 *		FLGData2D c = new FLGData2D("Daniel", 23);
 * </pre><p>
 * @see	FLGDataGroup2D
 * @see FLGBarChart2D
 */
public class FLGData2D {
    /** Der Name dieses Datenobjektes. */
    private String name;

    /** Der Wert dieses Datenobjektes. */
    private double value;

    /** Die Farbe dieses Datenobjektes. */
    private Color color;

    /**
     * Erzeugt ein <code>FLGData2D</code> mit dem spezifizierten Namen und Wert.
     * @param	name	der Name.
     * @param	value	der Wert.
     */
    public FLGData2D(String name, double value) {
        this.name = name;
        this.value = value;
        this.color = null;
    }

    /**
     * Erzeugt ein <code>FLGData2D</code> mit dem spezifizierten Namen und Wert und der zugeordneten spezifizierten Farbe.
     * @param	name	der Name.
     * @param	value	der Wert.
     * @param color die Farbe.
     */
    public FLGData2D(String name, double value, Color color) {
        this.name = name;
        this.value = value;
        this.color = color;
    }

    /**
     * Liefert den Wert dieses Datenobjektes.
     * @return	den Wert.
     */
    public double getValue() { return value; }

    /**
     * Liefert den Namen dieses Datenobjektes.
     * @return	den Namen.
     */
    public String getName() { return name; }

    /**
     * Liefert die Farbe dieses Datenobjektes.
     * @return	die Farbe oder <code>null</code>, wenn keine Farbe definiert wurde.
     */
    public Color getColor() { return color; }

    /**
     * Setzt den Wert dieses Datenobjektes.
     * @param	value	der neue Wert.
     */
    public void setValue(double value) { this.value = value; }

    /**
     * Setzt den Namen dieses Datenobjektes.
     * @param	name	der neue Name.
     */
    public void setName(String name) { this.name = name; }

    /**
     * Setzt die Farbe dieses Datenobjektes.
     * @param	color die neue Farbe.
     */
    public void setColor(Color color) { this.color = color; }
}
